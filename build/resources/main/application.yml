spring:
  application:
    name: genesis
  datasource:
    url: jdbc:mysql://127.0.0.1:32768/genesis
    username: root
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
server:
  port: 8080
  error:
    include-stacktrace: never
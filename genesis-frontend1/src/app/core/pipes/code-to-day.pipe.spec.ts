import { CodeToDayPipe } from './code-to-day.pipe';

describe('CodeToDayPipe', () => {
  it('create an instance', () => {
    const pipe = new CodeToDayPipe();
    expect(pipe).toBeTruthy();
  });
});

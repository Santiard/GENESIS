import { CodeToHourPipe } from './code.to.hour.pipe';

describe('CodeToHourPipe', () => {
  it('create an instance', () => {
    const pipe = new CodeToHourPipe();
    expect(pipe).toBeTruthy();
  });
});
